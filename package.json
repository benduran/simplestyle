{
  "name": "simplestyle-js",
  "version": "2.6.0",
  "description": "An incredibly straightforward and simple CSS-in-JS solution with zero runtime dependencies, and out-of-the-box TypeScript support",
  "main": "dist/index.js",
  "module": "dist/simplestyle-js.esm.js",
  "esnext": "dist/simplestyle-js.esm.js",
  "typings": "dist/index.d.ts",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/benduran/simplestyle.git"
  },
  "scripts": {
    "build": "rm -rf ./dist && tsc --project ./tsconfig.json && tsc --project ./tsconfig.commonjs.json && yarn fixsourcemaps",
    "coveralls": "run-s test:coverage coverage:pipe",
    "coverage:pipe": "cat ./coverage/lcov.info | coveralls",
    "fixsourcemaps": "node ./fixSourceMaps.js",
    "lint": "eslint './src/**/*.ts'",
    "lint:fixall": "eslint './src/**/*.ts' --fix",
    "test:coverage": "jest --coverage",
    "test": "rm -rf ./dist && jest",
    "test:watch": "rm -rf ./dist && jest --watch",
    "prepublishOnly": "yarn build",
    "git-pre-commit": "yarn lint && yarn test"
  },
  "keywords": [
    "CSS-in-JS",
    "CSS",
    "Style",
    "Styled",
    "Simple"
  ],
  "author": "Benjamin Duran <stratodyne@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/autoprefixer": "^10.2.0",
    "@types/jest": "^26.0.23",
    "@types/jsdom": "^16.2.10",
    "@zeit/git-hooks": "^0.1.4",
    "autoprefixer": "^10.2.5",
    "babel-jest": "^26.6.3",
    "coveralls": "^3.1.0",
    "eslint-config-react-yas": "1.1.1",
    "jest": "^26.6.3",
    "jsdom": "^16.5.3",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.2.15",
    "ts-jest": "^26.5.6",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "csstype": "^3.0.8"
  }
}
